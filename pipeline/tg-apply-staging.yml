---
trigger:
  branches:
    include:
    - 'main'
  paths:
    include:
    - environments/staging

pr: none

pool: 'LaCC PreProd Pool'

jobs:
  - template: templates/terragrunt_job.yml
    parameters:
      environment: staging
      module: applications
      subscription: "Azure Pipeline: Large and Complex Cases - Pre-Prod" #service principal
      variableGroupName: 'lacc-infra-vars-staging'
      runApply: true

  - template: templates/terragrunt_job.yml
    parameters:
      environment: staging
      module: base
      subscription: "Azure Pipeline: Large and Complex Cases - Pre-Prod" #service principal
      runApply: true
